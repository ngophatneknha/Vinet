OWNER_EMAIL=owner@example.com
# Optional one-time activation fallback, otherwise keep unset.
SETUP_CODE=
# Optional scoped import credential; does not bypass Sites access.
IMPORT_TOKEN=
# Exact deployed origin for social preview.
SITE_ORIGIN=
